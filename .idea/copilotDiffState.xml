<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/main/src/game.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/main/src/game.py" />
              <option name="originalContent" value="import sys&#10;from constants import *&#10;from graph import Graph&#10;&#10;# Verifica término: nenhum jogador consegue mover&#10;def qualquer_pode_jogar(graph: Graph):&#10;    global temporary_number_of_players&#10;&#10;    for i, cor in enumerate(PLAYER_COLORS):&#10;        for face in graph.faces:&#10;&#10;            # verifica se ultrapassou o número máximo de jogadores&#10;            if i &gt;= temporary_number_of_players:   ##############&#10;                print(&quot;número máximo de jogadores: &quot;, temporary_number_of_players)&#10;                break&#10;&#10;            if face['color'] is not None:&#10;                continue&#10;            verts = face['vertices']&#10;            edges_f = set(tuple(sorted((verts[k], verts[(k+1)%len(verts)]))) for k in range(len(verts)))&#10;            bloqueado = False&#10;            for other in graph.faces:&#10;                if other['color'] is None or other is face:&#10;                    continue&#10;                ov = other['vertices']&#10;                edges_o = set(tuple(sorted((ov[k], ov[(k+1)%len(ov)]))) for k in range(len(ov)))&#10;                if edges_f &amp; edges_o and other['color']==cor:&#10;                    bloqueado = True&#10;                    # Mensagem de indicação de bloqueio&#10;                    break&#10;            if not bloqueado:&#10;                return True&#10;    return False&#10;&#10;# Tela de jogo&#10;def play(screen, graph: Graph, events):&#10;    global temporary_number_of_players&#10;    # Variáveis estáticas para manter estado entre chamadas&#10;    if not hasattr(play, &quot;turn&quot;):&#10;        play.turn = 0&#10;        play.finished = False&#10;        play.winner = None&#10;    screen.fill(BG_COLOR)&#10;    &#10;    # Desenha grafo (faces preenchidas e arestas)&#10;    graph.desenhar(screen, cor_arestas=SHAPES_COLOR, largura_arestas=1)&#10;    &#10;    # Indica turno atual na frente&#10;    turno_text = f&quot;Vez de: {player_names[play.turn]}&quot;&#10;    turno_surf = FONT_MEDIUM.render(turno_text, True, PLAYER_COLORS[play.turn])&#10;    screen.blit(turno_surf, (20, 20))&#10;    &#10;    # Processa eventos&#10;    for event in events:&#10;        if event.type == pygame.QUIT:&#10;            pygame.quit()&#10;            sys.exit()&#10;        if play.finished:&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1 and PLAY_BUTTON.collidepoint(event.pos):&#10;                # voltar ao menu&#10;                play.turn = 0&#10;                play.finished = False&#10;                play.winner = None&#10;                return False&#10;        else:&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:&#10;                print(&quot;Vez de jogador:&quot;, play.turn)&#10;                pos = event.pos&#10;                for idx, face in enumerate(graph.faces):&#10;                    if graph._ponto_no_poligono(pos, face['vertices']) and face['color'] is None:&#10;&#10;                        cor = PLAYER_COLORS[play.turn]&#10;&#10;                        # checa faces adjacentes (compartilham aresta)&#10;                        bloqueado = False&#10;&#10;                        # gera arestas da face clicada&#10;                        f_verts = face['vertices']&#10;&#10;                        # pega a lista de valores únicos de tuplas ordenadas que representam as arestas de uma face,&#10;                        #  geradas a partir de cada par de vértices consecutivos (incluindo a conexão do último ao primeiro).&#10;                        edges_f = set(tuple(sorted((f_verts[i], f_verts[(i+1)%len(f_verts)]))) for i in range(len(f_verts)))&#10;&#10;                        for j, other in enumerate(graph.faces):&#10;                            if j==idx or other['color'] is None:&#10;                                continue&#10;&#10;                            # arestas da outra face&#10;                            ov = other['vertices']&#10;&#10;                            # coleção de valores únicos de tuplas ordenadas que representam as arestas da outra face,&#10;                            #  geradas a partir de cada par de vértices consecutivos (incluindo a conexão do último ao primeiro).&#10;                            edges_o = set(tuple(sorted((ov[i], ov[(i+1)%len(ov)]))) for i in range(len(ov)))&#10;&#10;                            # se compartilham aresta&#10;                            if edges_f &amp; edges_o and other['color']==cor:&#10;                                bloqueado = True&#10;                                print(&quot;Jogador&quot;, play.turn, &quot;não pode colorir a face&quot;, idx, &quot;com a cor&quot;, cor, &quot;porque está bloqueado por outra face.&quot;)&#10;                                break&#10;&#10;                        if not bloqueado:&#10;                            graph.colorir_face(idx, cor)&#10;&#10;                            # Lógica de turno&#10;                            play.turn = (play.turn + 1) % temporary_number_of_players&#10;&#10;                        break&#10;    # Verifica jogadas possíveis&#10;    if not play.finished:&#10;        cor = PLAYER_COLORS[play.turn]&#10;        possible=False&#10;        for idx, face in enumerate(graph.faces):&#10;            if face['color'] is not None: continue&#10;&#10;            # monta arestas&#10;            verts = face['vertices']&#10;            edges_f = set(tuple(sorted((verts[i], verts[(i+1)%len(verts)]))) for i in range(len(verts)))&#10;&#10;            bloqueado=False&#10;            for j, other in enumerate(graph.faces):&#10;                if j==idx or other['color'] is None: continue&#10;                ov = other['vertices']&#10;                edges_o = set(tuple(sorted((ov[i], ov[(i+1)%len(ov)]))) for i in range(len(ov)))&#10;                if edges_f &amp; edges_o and other['color']==cor:&#10;                    bloqueado=True; break&#10;            if not bloqueado:&#10;                possible=True&#10;                break&#10;&#10;        if not qualquer_pode_jogar(graph):&#10;            play.finished = True&#10;            # conta faces por jogador&#10;            counts = [0] * (temporary_number_of_players + 1)  ############&#10;            print(&quot;Contando faces por jogador... -&gt; contagem: &quot;, counts, &quot; - jogadores: &quot;, temporary_number_of_players)&#10;            for face in graph.faces:&#10;                if face['color'] in PLAYER_COLORS:&#10;                    idx = PLAYER_COLORS.index(face['color'])&#10;                    counts[idx] += 1&#10;            max_faces = max(counts)&#10;            vencedores = [i for i, c in enumerate(counts) if c == max_faces]&#10;&#10;            # desempate: último que jogou (play.turn-1)&#10;            for offset in range(1, temporary_number_of_players + 1):  ###############&#10;                candidato = (play.turn - offset) % temporary_number_of_players&#10;                if candidato in vencedores:&#10;                    play.winner = candidato&#10;                    break&#10;&#10;    # Quando finalizado, mostra mensagem e botão&#10;    if play.finished:&#10;        msg = f&quot;{player_names[play.winner]} venceu!&quot;&#10;        msg_surf = FONT_LARGE.render(msg, True, PLAYER_COLORS[play.winner])&#10;&#10;        # exibe mensagem de vitória no topo centralizado&#10;        msg_rect = msg_surf.get_rect(midtop=(SCREEN_WIDTH//2, 20))&#10;        screen.blit(msg_surf, msg_rect)&#10;&#10;        color_btn = BUTTON_HOVER if PLAY_BUTTON.collidepoint(pygame.mouse.get_pos()) else BUTTON_COLOR&#10;        pygame.draw.rect(screen, color_btn, PLAY_BUTTON, border_radius=8)&#10;&#10;        label = FONT_MEDIUM.render(&quot;Voltar ao menu&quot;, True, TEXT_COLOR)&#10;        screen.blit(label, label.get_rect(center=PLAY_BUTTON.center))&#10;    pygame.display.update()&#10;    return True&#10;" />
              <option name="updatedContent" value="import sys&#10;import pygame&#10;import constants&#10;from graph import Graph&#10;&#10;def qualquer_pode_jogar(graph: Graph):&#10;    for i, cor in enumerate(constants.PLAYER_COLORS):&#10;        for face in graph.faces:&#10;&#10;            # verifica se este jogador está ativo&#10;            if i &gt;= constants.temporary_number_of_players:&#10;                break&#10;&#10;            if face['color'] is not None:&#10;                continue&#10;            verts = face['vertices']&#10;            edges_f = set(tuple(sorted((verts[k], verts[(k+1)%len(verts)]))) for k in range(len(verts)))&#10;            bloqueado = False&#10;            for other in graph.faces:&#10;                if other['color'] is None or other is face:&#10;                    continue&#10;                ov = other['vertices']&#10;                edges_o = set(tuple(sorted((ov[k], ov[(k+1)%len(ov)]))) for k in range(len(ov)))&#10;                if edges_f &amp; edges_o and other['color']==cor:&#10;                    bloqueado = True&#10;                    # Mensagem de indicação de bloqueio&#10;                    break&#10;            if not bloqueado:&#10;                return True&#10;    return False&#10;&#10;# Tela de jogo&#10;def play(screen, graph: Graph, events):&#10;    # Variáveis estáticas para manter estado entre chamadas&#10;    if not hasattr(play, &quot;turn&quot;):&#10;        play.turn = 0&#10;        play.finished = False&#10;        play.winner = None&#10;    screen.fill(constants.BG_COLOR)&#10;    &#10;    # Desenha grafo (faces preenchidas e arestas)&#10;    graph.desenhar(screen, cor_arestas=constants.SHAPES_COLOR, largura_arestas=1)&#10;    &#10;    # Indica turno atual na frente&#10;    turno_text = f&quot;Vez de: {constants.player_names[play.turn]}&quot;&#10;    turno_surf = constants.FONT_MEDIUM.render(turno_text, True, constants.PLAYER_COLORS[play.turn])&#10;    screen.blit(turno_surf, (20, 20))&#10;    # Verifica se jogador atual não tem jogadas; se sim, encerra jogo (último a jogar vence)&#10;    # Função inline para verificar jogadas&#10;    def player_can_move(idx):&#10;        cor = constants.PLAYER_COLORS[idx]&#10;        for face in graph.faces:&#10;            if face['color'] is not None: continue&#10;            # arestas da face&#10;            verts = face['vertices']&#10;            edges_f = set(tuple(sorted((verts[i], verts[(i+1)%len(verts)]))) for i in range(len(verts)))&#10;            # verifica bloqueio por mesma cor em face adjacente&#10;            blocked = False&#10;            for other in graph.faces:&#10;                if other is face or other['color'] is None: continue&#10;                ov = other['vertices']&#10;                edges_o = set(tuple(sorted((ov[i], ov[(i+1)%len(ov)]))) for i in range(len(ov)))&#10;                if edges_f &amp; edges_o and other['color'] == cor:&#10;                    blocked = True; break&#10;            if not blocked:&#10;                return True&#10;        return False&#10;&#10;    if not player_can_move(play.turn):&#10;        play.finished = True&#10;        play.winner = (play.turn - 1) % constants.temporary_number_of_players&#10;        # skip event processing, will draw vitória no bloco abaixo&#10;        # ...existing code continues to draw end screen...&#10;    # Processa eventos&#10;    for event in events:&#10;        if event.type == pygame.QUIT:&#10;            pygame.quit()&#10;            sys.exit()&#10;        if play.finished:&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1 and constants.PLAY_BUTTON.collidepoint(event.pos):&#10;                # voltar ao menu&#10;                play.turn = 0&#10;                play.finished = False&#10;                play.winner = None&#10;                return False&#10;        else:&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:&#10;                print(&quot;Vez de jogador:&quot;, play.turn)&#10;                pos = event.pos&#10;                for idx, face in enumerate(graph.faces):&#10;                    if graph._ponto_no_poligono(pos, face['vertices']) and face['color'] is None:&#10;&#10;                        cor = constants.PLAYER_COLORS[play.turn]&#10;&#10;                        # checa faces adjacentes (compartilham aresta)&#10;                        bloqueado = False&#10;&#10;                        # gera arestas da face clicada&#10;                        f_verts = face['vertices']&#10;&#10;                        # pega a lista de valores únicos de tuplas ordenadas que representam as arestas de uma face,&#10;                        #  geradas a partir de cada par de vértices consecutivos (incluindo a conexão do último ao primeiro).&#10;                        edges_f = set(tuple(sorted((f_verts[i], f_verts[(i+1)%len(f_verts)]))) for i in range(len(f_verts)))&#10;&#10;                        for j, other in enumerate(graph.faces):&#10;                            if j==idx or other['color'] is None:&#10;                                continue&#10;&#10;                            # arestas da outra face&#10;                            ov = other['vertices']&#10;&#10;                            # coleção de valores únicos de tuplas ordenadas que representam as arestas da outra face,&#10;                            #  geradas a partir de cada par de vértices consecutivos (incluindo a conexão do último ao primeiro).&#10;                            edges_o = set(tuple(sorted((ov[i], ov[(i+1)%len(ov)]))) for i in range(len(ov)))&#10;&#10;                            # se compartilham aresta&#10;                            if edges_f &amp; edges_o and other['color']==cor:&#10;                                bloqueado = True&#10;                                print(&quot;Jogador&quot;, play.turn, &quot;não pode colorir a face&quot;, idx, &quot;com a cor&quot;, cor, &quot;porque está bloqueado por outra face.&quot;)&#10;                                break&#10;&#10;                        if not bloqueado:&#10;                            graph.colorir_face(idx, cor)&#10;&#10;                            # Lógica de turno&#10;                            play.turn = (play.turn + 1) % constants.temporary_number_of_players&#10;&#10;                        break&#10;    # Quando finalizado, mostra mensagem no topo e botão&#10;    if play.finished:&#10;        msg = f&quot;{constants.player_names[play.winner]} venceu!&quot;&#10;        msg_surf = constants.FONT_LARGE.render(msg, True, constants.PLAYER_COLORS[play.winner])&#10;&#10;        # exibe mensagem de vitória no topo centralizado&#10;        msg_rect = msg_surf.get_rect(midtop=(constants.SCREEN_WIDTH//2, 20))&#10;        screen.blit(msg_surf, msg_rect)&#10;&#10;        color_btn = constants.BUTTON_HOVER if constants.PLAY_BUTTON.collidepoint(pygame.mouse.get_pos()) else constants.BUTTON_COLOR&#10;        pygame.draw.rect(screen, color_btn, constants.PLAY_BUTTON, border_radius=8)&#10;&#10;        label = constants.FONT_MEDIUM.render(&quot;Voltar ao menu&quot;, True, constants.TEXT_COLOR)&#10;        screen.blit(label, label.get_rect(center=constants.PLAY_BUTTON.center))&#10;    pygame.display.update()&#10;    return True" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/main/src/main.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/main/src/main.py" />
              <option name="originalContent" value="import sys&#10;import pygame&#10;from constants import *&#10;from graph import Graph&#10;import shapes as shapes_module&#10;from game import play&#10;from menu import draw_menu&#10;&#10;# Reinicialização&#10;pygame.init()&#10;&#10;# Janela [1]&#10;SCREEN = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))&#10;pygame.display.set_caption(&quot;COL&quot;)&#10;&#10;# Loop de jogo [2]&#10;running = True&#10;game_started = False&#10;&#10;shapes = None  # receberá lista de shapes da partida&#10;graph: Graph = None&#10;&#10;while running:&#10;    global temporary_number_of_players&#10;    events = pygame.event.get()&#10;&#10;    if not game_started:&#10;        for event in events:&#10;            if event.type == pygame.QUIT:&#10;                running = False&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:&#10;                if PLAY_BUTTON.collidepoint(event.pos):&#10;                    print(f&quot;Iniciando jogo com {temporary_number_of_players} jogadores!&quot;)&#10;                    # gera novas shapes aleatórias a cada partida&#10;                    shapes = shapes_module.generate_shapes()&#10;                    graph = Graph(shapes, area_minima_face=100)&#10;                    game_started = True&#10;        draw_menu(SCREEN, events)&#10;        pygame.display.update()&#10;    else:&#10;        cont = play(SCREEN, graph, events)&#10;        if not cont:&#10;            game_started = False&#10;&#10;# Encerrar pygame&#10;pygame.quit()&#10;sys.exit()&#10;" />
              <option name="updatedContent" value="import sys&#10;import pygame&#10;import constants&#10;from graph import Graph&#10;import shapes as shapes_module&#10;from game import play&#10;from menu import draw_menu&#10;&#10;# Reinicialização&#10;pygame.init()&#10;&#10;# Janela [1]&#10;SCREEN = pygame.display.set_mode((constants.SCREEN_WIDTH, constants.SCREEN_HEIGHT))&#10;pygame.display.set_caption(&quot;COL&quot;)&#10;&#10;# Loop de jogo [2]&#10;running = True&#10;game_started = False&#10;&#10;shapes = None  # receberá lista de shapes da partida&#10;graph: Graph = None&#10;&#10;while running:&#10;    events = pygame.event.get()&#10;&#10;    if not game_started:&#10;        for event in events:&#10;            if event.type == pygame.QUIT:&#10;                running = False&#10;            if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:&#10;                if constants.PLAY_BUTTON.collidepoint(event.pos):&#10;                    print(f&quot;Iniciando jogo com {constants.temporary_number_of_players} jogadores!&quot;)&#10;                    shapes = shapes_module.generate_shapes()&#10;                    graph = Graph(shapes, area_minima_face=100)&#10;                    game_started = True&#10;        draw_menu(SCREEN, events)&#10;        pygame.display.update()&#10;    else:&#10;        cont = play(SCREEN, graph, events)&#10;        if not cont:&#10;            game_started = False&#10;&#10;# Encerrar pygame&#10;pygame.quit()&#10;sys.exit()" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/main/src/menu.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/main/src/menu.py" />
              <option name="originalContent" value="import pygame&#10;from constants import *&#10;&#10;# Logo&#10;try:&#10;    logo_img = pygame.image.load(r'C:/Users/conta/Documents/Programacao/Outros/TCC/main/assets/col-logo.png')&#10;    logo_img = pygame.transform.smoothscale(logo_img, (1600, 900))     # Redimensionar a imagem&#10;except pygame.error:&#10;    logo_img = None  # se falhar ao carregar, seguimos sem imagem&#10;&#10;# Menu inicial&#10;def draw_menu(screen: pygame.Surface, events: list[pygame.event.Event]):&#10;    global temporary_number_of_players&#10;&#10;    # Processar eventos&#10;    for event in events:&#10;        if event.type == pygame.MOUSEBUTTONDOWN:&#10;            if event.button == 1:  # Clique esquerdo&#10;                mouse_pos = event.pos&#10;&#10;                # Verificar clique na seta esquerda&#10;                if LEFT_ARROW.collidepoint(mouse_pos):&#10;                    if temporary_number_of_players &gt; min_players:&#10;                        temporary_number_of_players -= 1&#10;                    print(f&quot;número de jogadores: {temporary_number_of_players}&quot;)&#10;&#10;                # Verificar clique na seta direita&#10;                elif RIGHT_ARROW.collidepoint(mouse_pos):&#10;                    if temporary_number_of_players &lt; max_players:&#10;                        temporary_number_of_players += 1&#10;                    print(f&quot;número de jogadores: {temporary_number_of_players}&quot;)&#10;&#10;                # Verificar clique no botão JOGAR&#10;                elif PLAY_BUTTON.collidepoint(mouse_pos):&#10;                    print(f&quot;Iniciando jogo com {temporary_number_of_players} jogadores!&quot;)&#10;                    return &quot;start_game&quot;  # Retorna sinal para iniciar o jogo&#10;&#10;    screen.fill(BG_COLOR)&#10;&#10;    # Desenhar logo, se carregado&#10;    if logo_img:&#10;        logo_rect = logo_img.get_rect(center=(SCREEN_WIDTH // 2, 140))&#10;        screen.blit(logo_img, logo_rect)&#10;&#10;    # Título&#10;    title_surf = FONT_MEDIUM.render(&quot;Número de jogadores&quot;, True, TEXT_COLOR)  # Posicionamento vertical do texto &quot;nº de jogadores&quot;&#10;    title_pos = title_surf.get_rect(center=(SCREEN_WIDTH//2, 105))&#10;    screen.blit(title_surf, title_surf.get_rect(center=(SCREEN_WIDTH // 2, SCREEN_HEIGHT // 2)))&#10;&#10;    # Setas e número atual&#10;    arrows_y = title_pos.bottom + 210&#10;    LEFT_ARROW.topleft = (SCREEN_WIDTH // 2 - 100, arrows_y)&#10;    RIGHT_ARROW.topleft = (SCREEN_WIDTH // 2 + 60, arrows_y)&#10;&#10;    # Seta para esquerda&#10;    left_color = BUTTON_HOVER if LEFT_ARROW.collidepoint(pygame.mouse.get_pos()) else TEXT_COLOR&#10;    pygame.draw.polygon(screen, left_color, [&#10;        (LEFT_ARROW.right, LEFT_ARROW.top),&#10;        (LEFT_ARROW.left, LEFT_ARROW.centery),&#10;        (LEFT_ARROW.right, LEFT_ARROW.bottom),&#10;    ])&#10;    # Seta para direita&#10;    right_color = BUTTON_HOVER if RIGHT_ARROW.collidepoint(pygame.mouse.get_pos()) else TEXT_COLOR&#10;    pygame.draw.polygon(screen, right_color, [&#10;        (RIGHT_ARROW.left, RIGHT_ARROW.top),&#10;        (RIGHT_ARROW.right, RIGHT_ARROW.centery),&#10;        (RIGHT_ARROW.left, RIGHT_ARROW.bottom),&#10;    ])&#10;&#10;    # Número (centro entre as setas)&#10;    num_surf = FONT_MEDIUM.render(str(temporary_number_of_players), True, TEXT_COLOR)&#10;    num_pos = num_surf.get_rect(center=(SCREEN_WIDTH // 2, arrows_y + LEFT_ARROW.height // 2))&#10;    screen.blit(num_surf, num_pos)&#10;&#10;    # Lista de jogadores em linha&#10;    count = temporary_number_of_players&#10;    spacing = 75&#10;    total_width = (count - 1) * spacing&#10;    start_x = SCREEN_WIDTH//2 - total_width//2&#10;    y_text = 400  # Posicionamento vertical da lista de jogadores&#10;&#10;    for i in range(count):&#10;        dimension_square = 30&#10;        x = start_x + i * spacing&#10;&#10;        # Nomes&#10;        name_surf = FONT_SMALL.render(player_names[i], True, TEXT_COLOR)&#10;        screen.blit(name_surf, name_surf.get_rect(center=(x, y_text)))&#10;&#10;        # Quadrado de cor ao lado&#10;        square = pygame.Rect(x + name_surf.get_width()//2 - name_surf.get_width() + 15, y_text + dimension_square//2, dimension_square, dimension_square)&#10;        pygame.draw.rect(screen, PLAYER_COLORS[i], square)&#10;        pygame.draw.rect(screen, TEXT_COLOR, square, 2)&#10;&#10;    # Botão &quot;JOGAR&quot;&#10;    mouse = pygame.mouse.get_pos()&#10;    btn_color = BUTTON_HOVER if PLAY_BUTTON.collidepoint(mouse) else BUTTON_COLOR&#10;    pygame.draw.rect(screen, btn_color, PLAY_BUTTON, border_radius=8)&#10;    play_surf = FONT_MEDIUM.render(&quot;JOGAR&quot;, True, TEXT_COLOR)&#10;    screen.blit(play_surf, play_surf.get_rect(center=PLAY_BUTTON.center))&#10;&#10;    return None  # Retorna None quando não há ação especial&#10;" />
              <option name="updatedContent" value="import pygame&#10;import constants&#10;&#10;# Logo&#10;try:&#10;    logo_img = pygame.image.load(r'C:/Users/conta/Documents/Programacao/Outros/TCC/main/assets/col-logo.png')&#10;    logo_img = pygame.transform.smoothscale(logo_img, (1600, 900))     # Redimensionar a imagem&#10;except pygame.error:&#10;    logo_img = None  # se falhar ao carregar, seguimos sem imagem&#10;&#10;# Menu inicial&#10;def draw_menu(screen: pygame.Surface, events: list[pygame.event.Event]):&#10;    # no local temp, use constants.temporary_number_of_players&#10;&#10;    # Processar eventos&#10;    for event in events:&#10;        if event.type == pygame.MOUSEBUTTONDOWN:&#10;            if event.button == 1:  # Clique esquerdo&#10;                mouse_pos = event.pos&#10;&#10;                # Verificar clique na seta esquerda&#10;                if constants.LEFT_ARROW.collidepoint(mouse_pos):&#10;                    if constants.temporary_number_of_players &gt; constants.min_players:&#10;                        constants.temporary_number_of_players -= 1&#10;                    print(f&quot;número de jogadores: {constants.temporary_number_of_players}&quot;)&#10;&#10;                # Verificar clique na seta direita&#10;                elif constants.RIGHT_ARROW.collidepoint(mouse_pos):&#10;                    if constants.temporary_number_of_players &lt; constants.max_players:&#10;                        constants.temporary_number_of_players += 1&#10;                    print(f&quot;número de jogadores: {constants.temporary_number_of_players}&quot;)&#10;&#10;                # Verificar clique no botão JOGAR&#10;                elif constants.PLAY_BUTTON.collidepoint(mouse_pos):&#10;                    print(f&quot;Iniciando jogo com {constants.temporary_number_of_players} jogadores!&quot;)&#10;                    return &quot;start_game&quot;  # Retorna sinal para iniciar o jogo&#10;&#10;    screen.fill(constants.BG_COLOR)&#10;&#10;    # Desenhar logo, se carregado&#10;    if logo_img:&#10;        logo_rect = logo_img.get_rect(center=(constants.SCREEN_WIDTH // 2, 140))&#10;        screen.blit(logo_img, logo_rect)&#10;&#10;    # Título&#10;    title_surf = constants.FONT_MEDIUM.render(&quot;Número de jogadores&quot;, True, constants.TEXT_COLOR)  # Posicionamento vertical do texto &quot;nº de jogadores&quot;&#10;    title_pos = title_surf.get_rect(center=(constants.SCREEN_WIDTH//2, 105))&#10;    screen.blit(title_surf, title_surf.get_rect(center=(constants.SCREEN_WIDTH // 2, constants.SCREEN_HEIGHT // 2)))&#10;&#10;    # Setas e número atual&#10;    arrows_y = title_pos.bottom + 210&#10;    constants.LEFT_ARROW.topleft = (constants.SCREEN_WIDTH // 2 - 100, arrows_y)&#10;    constants.RIGHT_ARROW.topleft = (constants.SCREEN_WIDTH // 2 + 60, arrows_y)&#10;&#10;    # Seta para esquerda&#10;    left_color = constants.BUTTON_HOVER if constants.LEFT_ARROW.collidepoint(pygame.mouse.get_pos()) else constants.TEXT_COLOR&#10;    pygame.draw.polygon(screen, left_color, [&#10;        (constants.LEFT_ARROW.right, constants.LEFT_ARROW.top),&#10;        (constants.LEFT_ARROW.left, constants.LEFT_ARROW.centery),&#10;        (constants.LEFT_ARROW.right, constants.LEFT_ARROW.bottom),&#10;    ])&#10;    # Seta para direita&#10;    right_color = constants.BUTTON_HOVER if constants.RIGHT_ARROW.collidepoint(pygame.mouse.get_pos()) else constants.TEXT_COLOR&#10;    pygame.draw.polygon(screen, right_color, [&#10;        (constants.RIGHT_ARROW.left, constants.RIGHT_ARROW.top),&#10;        (constants.RIGHT_ARROW.right, constants.RIGHT_ARROW.centery),&#10;        (constants.RIGHT_ARROW.left, constants.RIGHT_ARROW.bottom),&#10;    ])&#10;&#10;    # Número (centro entre as setas)&#10;    num_surf = constants.FONT_MEDIUM.render(str(constants.temporary_number_of_players), True, constants.TEXT_COLOR)&#10;    num_pos = num_surf.get_rect(center=(constants.SCREEN_WIDTH // 2, arrows_y + constants.LEFT_ARROW.height // 2))&#10;    screen.blit(num_surf, num_pos)&#10;&#10;    # Lista de jogadores em linha&#10;    count = constants.temporary_number_of_players&#10;    spacing = 75&#10;    total_width = (count - 1) * spacing&#10;    start_x = constants.SCREEN_WIDTH//2 - total_width//2&#10;    y_text = 400  # Posicionamento vertical da lista de jogadores&#10;&#10;    for i in range(constants.temporary_number_of_players):&#10;        dimension_square = 30&#10;        x = start_x + i * spacing&#10;&#10;        # Nomes&#10;        name_surf = constants.FONT_SMALL.render(constants.player_names[i], True, constants.TEXT_COLOR)&#10;        screen.blit(name_surf, name_surf.get_rect(center=(x, y_text)))&#10;&#10;        # Quadrado de cor ao lado&#10;        square = pygame.Rect(x + name_surf.get_width()//2 - name_surf.get_width() + 15, y_text + dimension_square//2, dimension_square, dimension_square)&#10;        pygame.draw.rect(screen, constants.PLAYER_COLORS[i], square)&#10;        pygame.draw.rect(screen, constants.TEXT_COLOR, square, 2)&#10;&#10;    # Botão &quot;JOGAR&quot;&#10;    mouse = pygame.mouse.get_pos()&#10;    btn_color = constants.BUTTON_HOVER if constants.PLAY_BUTTON.collidepoint(mouse) else constants.BUTTON_COLOR&#10;    pygame.draw.rect(screen, btn_color, constants.PLAY_BUTTON, border_radius=8)&#10;    play_surf = constants.FONT_MEDIUM.render(&quot;JOGAR&quot;, True, constants.TEXT_COLOR)&#10;    screen.blit(play_surf, play_surf.get_rect(center=constants.PLAY_BUTTON.center))&#10;&#10;    return None  # Retorna None quando não há ação especial" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>